platform: uk_transport
app_id: !secret transport_api_id
app_key: !secret transport_api_key
scan_interval: 210
queries:
  - mode: bus
    origin: !secret bus_stop
    destination: !secret bus_dest